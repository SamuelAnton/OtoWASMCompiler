class Calc is
    method sum(a: Integer, b: Integer) : Integer => a.Plus(b)
    method sum(a: Real, b: Real) : Real => a.Plus(b)
end

class Main is
    method main() : Real is
        var c : Calc();
        return c.sum(Integer(1), Integer(2));
        c.sum(Real(1.5), Real(2.5));
    end
end
