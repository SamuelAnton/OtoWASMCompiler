class Main is
    method main() : Integer is
        var m : Math();
        return m.square(Integer(4));
    end
end

class Math is
    method square(x: Integer) : Integer is
        return x.Mult(x);
    end
end
