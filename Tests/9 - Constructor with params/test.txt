class Point is
    var x : Integer(0)
    var y : Integer(0)

    this(a: Integer, b: Integer) is
        x := a;
        y := b;
    end
end

class Main is
    method main() : Integer is
        var p : Point(Integer(5), Integer(2));
        return p.y;
    end
end