class Counter is
    var value : Integer()

    this(x: Integer) is
        value := x;
    end

    method get() : Integer => value
end

class Tester is
    this() is
        var c : Counter(Integer(10));
        c.get();
    end
end